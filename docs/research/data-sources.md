# Free Data Sources for the MLE Engine — Research Survey

Spec: `league-translation-build-spec.md` (2026-09-17). Goal: strengthen `src/rosetta/ingest/`
beyond the current baseline (pybaseball/FanGraphs, npb.jp, mykbostats.com, cpbl.com.tw,
BR/Béisbol-de-Cuba, BR minors, Chadwick register, home/road splits).

Schema target: `SeasonLine` in `src/rosetta/schema.py` — hitter rates
(`pa, g, bb_pct, k_pct, iso, babip, hr_pct, avg, obp, slg, woba`),
pitcher rates (`ip, g, gs, k_pct, bb_pct, hr_fb, era, fip`),
park-split columns (`home_pa, road_pa, home_woba, road_woba, home_era, road_era, park_id`),
plus `player_id / player_name / age` for cohort linking.

All access methods below were **verified live on 2026-09-18** (HTTP checks, official docs,
GitHub API) unless marked otherwise. No guessed URLs.

---

## 1. Comparison table

| # | Source | Coverage | Access (verified) | Auth / ToS / Rate limits | Schema fill | Reproducibility |
|---|--------|----------|-------------------|--------------------------|-------------|-----------------|
| 1 | **MLB Stats API** (`statsapi.mlb.com`) | MLB (sport 1) · AAA (11) · AA (12) · A+ (13) · A (14) · Rookie (16) · Winter (17: LIDOM 131, LMP 132, LBPRC/PWL 133, LVBP 135, AFL 119, ABL 595, Caribbean Series 162) · Indy (23: Mexican League 125, Atlantic 436, Frontier 442, Pioneer 128, American Assoc 6045) · NPB (31) / KBO (32) sport entries exist but **return zero stat splits** | `GET /api/v1/stats?stats=season&group=hitting\|pitching&season=YYYY&sportId=..&leagueIds=..&limit=N` (JSON). Verified working for AAA-2024, LIDOM-2024 (21 splits), LMB-2024 (131 splits). League catalog: `/api/v1/leagues?sportId=..`. Docs: community OpenAPI mirrors + `pymlb-statsapi` schema set (official beta docs offline) | No auth. Unofficial/undocumented — MLB tolerates research use, no SLA, endpoints can change. Be polite: sequential requests, small `limit` + pagination, cache responses | Season lines for AAA/AA/A (full counting stats → derive all rate cols), winter leagues, LMB, Atlantic League. `personId` + venue/team fields → cohort IDs + `park_id`. No direct home/road splits at season level (use game logs / hydrations) | ✅ Snapshot-cacheable (JSON → CSV). CI-safe **only** against committed snapshots; live calls are a `fetch-*` step, never in `make backtest` |
| 2 | **Baseball-Reference Register** (all non-MLB pro + college) | NPB 1936–2026 (8,404 players, 1,227 w/ MLB) · KBO 1982–2026 (3,608 / 484) · Mexican League 1925–2026 (11,337 / 2,456) · CPBL (league pages e.g. `register/league.cgi?id=60f837d0`) · Cuban Series · winter/indy/college/summer-collegiate. Data licensed from 24-7 Baseball + Chadwick Bureau | League pages (`baseball-reference.com/register/league.cgi?id=..`) → full-season batting/pitching tables; per-league top-100 Batting/Pitching Leaders (`register/leader.cgi?type=bat&id=..`); every table has **"Get table as CSV"** share/export; player pages carry `key_bbref`-compatible IDs + gamelogs/splits links | No auth. Scraping is tolerated at low volume but BR rate-limits aggressively (429s) and ToS discourages bots — use CSV export links, 5–10s delays, UA string, snapshot everything | Season lines for **every** foreign/winter/indy/college league (one client, uniform tables). Player IDs link to Chadwick `key_bbref`/`key_bbref_minors`. Leader tables are top-100 only (not full-season) — full tables live on league pages | ✅ Snapshot-cacheable (CSV exports). Not CI-live (rate limits) — commit snapshots |
| 3 | **BR home/road splits via `pybaseball.get_splits`** | Any player with a BR page (MLB + MiLB + registered foreign players). Career or single-season | `from pybaseball import get_splits; get_splits('troutmi01', year, player_info=True, pitching_splits=True)` → multi-index DataFrame incl. Home/Road rows (verified in pybaseball `docs/split_stats.md`). Wraps `baseball-reference.com/players/split.fcgi` | Inherits BR scraping ToS (see #2). Player-by-player = many requests → slowest source; snapshot + long cache essential | **Directly fills `home_pa, road_pa, home_woba, road_woba, home_era, road_era`** — the park-factor inputs. `player_info=True` gives handedness/position (role-model help) | ⚠️ Snapshot-only. Per-player crawl; never in CI. Prioritize movers + park anchors, not full leagues |
| 4 | **Baseball Savant minor-league Statcast** | AAA: all games since 2023 (+ PCL/Charlotte home 2022); Single-A Florida State League since 2021. MLB Statcast 2008–(PX) / 2015+ | `baseballsavant.mlb.com/statcast-search-minors` (UI) + CSV download (docs: `baseballsavant.mlb.com/csv-docs`, both verified 200). Python: `baseball-stats-python` `minor_statcast_search`; R: `baseballr::statcast_search_minors()` | No auth. Undocumented CSV endpoints; heavy queries time out — shard by date/player, cache CSVs. Savant ToS: personal/research use | Phase-2 **pitch-level** AAA data (velo, spin, exit velo, xwOBA) → validates K%/BB%/HR translations; per-pitch aggregation can rebuild season rate cols for covered levels | ✅ Snapshot-cacheable (CSV). CI-safe as committed aggregates only |
| 5 | **Lahman Database (via SABR)** | MLB only, 1871–2025 incl. Negro Leagues. Team + player season tables, standings, managers, postseason | `sabr.org/lahman-database/` → SQL / Access / comma-delimited CSV bundle (v2025 released 2026-01-02). Also `github.com/seanlahman/*` mirrors | Fully free, no auth, redistributable. Annual release cadence | Historical MLB baseline + backtest history (pre-Statcast eras); `people` table IDs (bbref Lahman IDs) aid cohort linking. No splits, no foreign leagues | ✅✅ Fully CI-safe — versioned bulk download, commit the bundle version |
| 6 | **Retrosheet** | MLB 1897–2025: 226k games, event (play-by-play) + box + game logs; annual + parsed-CSV offerings | `retrosheet.org/downloads/csvdownloads.html` — 7-file CSV set (`batting, pitching, fielding, plays, gameinfo, teamstats, allplayers`) + raw event files (`retrosheet.org/game.htm`). R: `baseballr::retrosheet_data()` (needs Chadwick `cwevent` CLI) | Free incl. commercial **iff** the attribution notice is displayed prominently (see their site). Bulk zips, no rate-limit concern | Game-level batting/pitching → aggregate to season lines; `gameinfo` (venue/umpire) + play-by-play → **empirical home/road + park effects** for MLB baseline; retro IDs join Chadwick `key_retro` | ✅✅ Fully CI-safe as versioned snapshots (large: ~725MB full CSV zip — vendor a slimmed aggregate) |
| 7 | **FanGraphs park factors + splits leaderboards** | MLB park factors by season (verified `fangraphs.com/park-factors` → 200) + batting/pitching splits leaderboards incl. home/road | Park factors page (HTML table, snapshot it). Splits via `pybaseball` FanGraphs clients (`batting_stats`/`pitching_stats` accept date ranges; `fg_*` team endpoints exist) | No auth. FanGraphs blocks aggressive bots (Cloudflare) — gentle scraping only, snapshot, never CI-live | Published park factors cross-check the derived home/road factors; FG leaderboards give wOBA/FIP-scale components consistent with the MLB baseline already in `mlb.py` | ✅ Snapshot-cacheable. Not CI-live |
| 8 | **Baseball Savant MLB park factors** | MLB, Statcast-era, batter/pitcher handedness splits | `baseballsavant.mlb.com/leaderboard/statcast-park-factors` (verified 200) | Same as #4 | Handed-adjusted park factors — finer than league-average; validates NPB/KBO derived factors methodology | ✅ Snapshot-cacheable |
| 9 | **LIDOM official stats (DIGIMETRICS)** | Dominican winter league, season back-catalog (year selector observed 2004–2025) | `estadisticas.lidom.com/` + `/Colectivo` — team/individual batting/pitching/fielding tables (HTML; scrape + snapshot). Cross-check with Stats API sport 17 / league 131 (verified working) | No auth observed. Third-party platform (DIGIMETRICS); scrape gently, prefer Stats API where overlapping | Winter-league season lines (LIDOM) with AVG/OBP/SLG/OPS columns → derive `bb_pct/k_pct/iso/babip/woba`; spans years Stats API may lack | ✅ Snapshot-cacheable. Scrape, don't CI-live |
| 10 | **CPBL official English stats** | CPBL all-time + seasonal leaderboards, batting + pitching | `en.cpbl.com.tw/stats/toplist` (season) and `en.cpbl.com.tw/stats/recordall` (all-time; **includes K%, BB%, BABIP, OPS+ columns** — verified live). Box scores at `en.cpbl.com.tw/box` | No auth observed. HTML scrape with delays + snapshot | Near-complete CPBL season lines incl. advanced-rate columns; recordall gives career context for age/cohort work | ✅ Snapshot-cacheable |
| 11 | **MyKBO Stats** | KBO live scores/schedules/standings/stats, current season (verified live 2026 season) | `mykbostats.com/` + `/games` — HTML, no public API observed; scrape + snapshot; cross-check vs BR KBO register (#2) | Fan-run English site; no stated API/ToS — treat as fragile, low volume, always snapshot | Current-season KBO lines to keep the leaderboard fresh between BR updates | ⚠️ Fragile (unofficial, no API). Snapshot-only, lowest refresh priority |
| 12 | **npb.jp (BIS)** | NPB official player register + seasonal stats (Japanese; `/bis/players/`, English mirror `/bis/eng/players/`) | HTML scrape (already in spec). Player IDs are the `key_npb` space in Chadwick | Official source; JP-language; scrape gently | Canonical NPB season lines; IDs join Chadwick `key_npb` | ✅ Snapshot-cacheable |
| 13 | **Cuban data beyond BR** | BR register (Cuban National Series) + `baseballdeCuba` (already in spec). No new free source found with mover-grade depth | Keep both; treat CNS with wide bands per spec §7 | Thin mover sample — no source fixes this; honesty > precision | CNS season lines (partial) | ⚠️ Thin; snapshot whatever exists |
| 14 | **College / Cape Cod / NCAA** | BR register includes NCAA D-I/II/III, NAIA, summer collegiate (per BR). NCAA official stats at `stats.ncaa.org`; `baseballr` has `ncaa_*()` + `espn_college_baseball_*()` scrapers (verified in package docs) | Via BR register tables (#2 pattern) for history; via `baseballr` as **reference implementation** (port patterns to Python, don't shell out to R in CI) | BR ToS as #2; NCAA site is scrape-tolerant at low volume | Phase-2 amateur lines (spec §3 Phase 2) with wide bands; college IDs are sparse — name+school matching | ⚠️ Snapshot-only; Phase 2 |
| 15 | **`baseballr` / `sportsdataverse` (reference, not dependency)** | Coverage mirrors the above (MLB API incl. `statcast_search_minors` + WBC route, ESPN/NCAA, FanGraphs, BR, Chadwick, Retrosheet) + pre-computed `load_*()` model datasets (Stuff+, OAA, framing, run expectancy) | `billpetti.github.io/baseballr/` docs; `github.com/BillPetti/baseballr`; `py.sportsdataverse.org` | Open source (reference only) | Implementation patterns for minors/WBC Statcast + NCAA; pre-computed datasets as **validation baselines**, not inputs | N/A (reference) |
| 16 | **Brooks Baseball** | MLB PITCHf/x era (2007–2016ish), pitch-classification-corrected | `brooksbaseball.net/pfxVB/pfx.php` (verified live; Baseball Prospectus property). Historical scrapers exist (`mattdennewitz/baseball-brooks-pitch-importer`) | Research-friendly historically; legacy system | Pitch-mix detail for historical MLB baseline only — superseded by Statcast (#4). **Low priority; skip unless backtesting pre-2017 arms** | ⚠️ Legacy; snapshot if ever needed |

---

## 2. Per-source verdicts (reproducibility)

- **CI-safe bulk downloads (vendor + pin version):** Lahman (#5), Retrosheet (#6), Chadwick register (existing — see §4 warning).
- **Snapshot-and-commit (fetch scripts, never CI-live):** MLB Stats API (#1), BR register CSVs (#2), BR splits (#3), Savant minors CSVs (#4), FG/Savant park factors (#7, #8), LIDOM (#9), CPBL (#10), MyKBO (#11), npb.jp (#12).
- **Phase-2 / thin-data:** Cuban extras (#13), college/Cape (#14), Brooks (#16).

`make backtest` must keep using `data/snapshots/` only — unchanged rule (see `scrapers.py` docstring).

## 3. Prioritized recommendations — wire these first

1. **MLB Stats API (#1) — AAA + winter +indy in one client.** Single biggest schema win: it fills the entire AAA bridge (the most trusted chain link, per spec §3), plus LIDOM/LVBP/LBPRC/LMP, the Mexican League, and the Atlantic League with one JSON client, no auth, and working season endpoints (verified). Also the only live-update-feasible source (nightly `fetch-*` → snapshot diff). **Wired 2026-09-18** (`src/rosetta/ingest/statsapi.py`, `rosetta fetch-statsapi`; stdlib-only, no extra deps). Coverage caveat found in wiring: the season endpoint returns a limited (effectively qualified) pool — AAA-2024 yields 136 hitting / 22 pitching splits — so it is a high-signal bridge sample + refresh feed, not a census. Committed snapshots stay synthetic until a real-data backtest comparison is explicitly run.
2. **BR Register CSV exports (#2) — NPB/KBO/CPBL/Cuba depth.** Uniform full-season tables for every foreign league where the Stats API returns nothing (NPB/KBO sport entries are empty — verified). `key_bbref` IDs plug straight into the Chadwick cohort join. Covers winter/indy/college as a bonus. **Status 2026-09-18: BLOCKED from this machine** — `baseball-reference.com` returns Cloudflare 1015 (rate-limit/ban) on every path, and `mykbostats.com` serves a Cloudflare challenge; the CPBL English mirror (`en.cpbl.com.tw`) 404s. Retry from an unblocked IP with 5–10s delays + CSV-export links before building the BR parser. **Instead wired: KBO official (koreabaseball.com)** — `src/rosetta/ingest/kbo.py` + `rosetta fetch-kbo`, stdlib-only, cookie-session viewstate postbacks for seasons back to 1982, Basic1+Basic2 merge, birth years from detail pages, `kbo-{playerId}` stable IDs. Verified live on 2023 (Son Ah-seop .339/30 rows, Peddie 2.00 ERA). Caveat: leaderboards are top-qualified only (30/17 rows), no pitcher GS, no home/road splits.
3. **BR home/road splits via `get_splits` (#3) — the park-factor columns.** Only source found that directly fills `home_woba/road_woba/home_era/road_era` per player. Crawl order: current leaderboard names → all movers in `transfers.csv` → team anchors. This unblocks spec §3 park factors without waiting for published NPB/KBO factors (which don't exist freely).
4. **Savant minor-league Statcast (#4) — Phase-2 pitch data + translation validation.** All-AAA-since-2023 coverage is exactly the bridge league; use it to validate that K%/BB% translate cleanly and to prototype pitch-level translation (spec §3 Phase 2) with real data instead of synthetic `home/road` placeholders (cf. `mlb.py`, which currently fabricates 50/50 splits).
5. **Lahman (#5) — CI-safe historical baseline.** Versioned, redistributable, zero network flakiness: pin the bundle, Loudly document the version, and use it for backtest history + ID linkage. (Retrosheet #6 is the runner-up: richer, but ~725MB full CSV — vendor slim aggregates, or defer until park-effect work needs play-by-play.)

## 4. Maintenance flag (resolved 2026-09-18)

**The Chadwick download URL in `src/rosetta/ingest/chadwick.py` was stale** (`.../data/people.csv` → 404; register is now sharded `people-0.csv … people-f.csv`). Fixed: `download_register()` now fetches + concatenates all 16 shards (~45MB slim CSV, 520k rows — verified live, incl. cross-league keys `key_mlbam`/`key_bbref`/`key_npb` for Ohtani), and `rosetta fetch-chadwick` exists as the explicit refresh step. Snapshot stays out of git; `make backtest` still uses `data/snapshots/` only.
